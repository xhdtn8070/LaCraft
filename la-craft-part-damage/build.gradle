dependencies {
    compileOnly project(':la-craft-util')
}

shadowJar {
    archiveBaseName.set('la-craft-part-damage')
    archiveVersion.set('0.1')  // Remove the version from the jar name
    archiveClassifier.set(null)  // Remove the classifier from the jar name
}